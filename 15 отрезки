p = list(range(3,38))
q = list(range(21,57))

for k in range(1,90):
    for l in range(1,999):
        a = list(range(l,l+k))
        flag = True
        for x in range(1,1000):
            flag = ((x in q) <= (x in p)) <= (not(x in a))
            if not flag:
                break
        if flag:
            print(len(a))
            break

